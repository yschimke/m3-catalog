# Kit vocabulary batch — rendered evidence

Backing `agent/kit-vocabulary`, held on its own branch so the images have a
stable URL without carrying binaries into `main`.

* `new-cells.png` — the two new cells beside their bases.
* `renders/` — every affected sticker after the change.

The text-field knob split moves no pixels: 34 of 34 shared renders are
byte-identical before and after.
