# App bar small-image — rendered evidence

Backing `agent/appbar-small-image`.

`app-bar.png` — the small app bar's base beside its new `small-image` cell.

The kit's node shows Figma's placeholder graphic, not a photograph, so the cell
draws the same motif this catalog already uses for carousel items rather than
committing a raster. Both carousels render byte-identically to `main` after the
motif moved to a shared composable.
